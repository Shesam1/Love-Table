<%= form_with(model: restaurant_location, local: true) do |form| %>
  <% if restaurant_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant_location.errors.count, "error") %> prohibited this restaurant_location from being saved:</h2>

      <ul>
      <% restaurant_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.number_field :location_id, id: :restaurant_location_location_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.number_field :restaurant_id, id: :restaurant_location_restaurant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
